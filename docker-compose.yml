services:
  api:
    build:
      context: ./backend
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in .env before starting the API}
    ports:
      - "8002:8002"
    restart: unless-stopped
